
services:
  - type: web
    name: nova-ai
    buildCommand: docker build -t nova-ai .  # Build the Docker image
    startCommand: docker run -p 8080:8080 nova-ai  # Run the Docker container
    plan: free
